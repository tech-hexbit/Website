import React from "react";
import PropTypes from "prop-types";

export default function RET12({ domain }) {
  return <div>RET12</div>;
}

RET12.propTypes = {
  domain: PropTypes.string.isRequired,
};
